apiVersion: v1
kind: Service
metadata:
  name: echo-server
  namespace: drl
  labels:
    app: echo-server
spec:
  selector:
    app: echo-server
  ports:
    - name: http
      port: 80
      targetPort: 10000
      protocol: TCP
  type: ClusterIP
