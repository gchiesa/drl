Completed: 2026-01-31

Implementation Summary:
- Added github.com/sblinch/kdl-go dependency for KDL parsing
- Created config.kdl schema with listen, membership, and logging sections
- Rewrote internal/config package with:
  - KDL file parsing
  - Environment variable overrides (DRL_ prefix) with legacy support
  - Precedence: Environment > KDL > Defaults
  - Validate() method for configuration validation
- Added --config / -c CLI flag to main.go
- Updated docker-compose.yaml to mount config.kdl
- All tests pass (32 tests, 93.9% coverage on config package)
- Linting passes

Validation Criteria Met:
1. CLI Test: `drl --config non-existent.kdl` returns clear error
2. Override Test: Environment variables correctly override KDL values
3. KDL Syntax: Parser handles KDL v1 features (nodes, properties, comments)
