version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - goimports
    - misspell

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/gchiesa/drl

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
